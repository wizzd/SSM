<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yi.dao.UserDao">
    <select id="getUser" resultType="com.yi.entity.UserDo">
        select * from user where name=#{0} and  password=#{1}
    </select>
    <insert id="addUser" >
        insert into user (name, password, tel, address, editTime) value  (#{name},#{passwoed},#{tel},#{address},#{editTime},#{createTime})

    </insert>
    <update id="updateUserInfo" parameterType="User">
        update user set set name=#{newUser.name} , password=#{newUser.password} , tel=#{newUser.tel},
        address=#{newUser.address} ,editTime=#{newUser.editTime}
         where name=#{oldUser.name} and tel= #{oldUser.password}
    </update>
</mapper>
